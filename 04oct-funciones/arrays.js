const numbers =   [ 1, 2, 3, 4, 5, 6,]// que pueda agarrar cualquier arreglo e invertirlo

const invertArray = (array) => { 
for (let contador = array.lenght -1; contador >=0 ; contador -- )
console.log(contador[numbers])  }
 
invertArray(numbers)